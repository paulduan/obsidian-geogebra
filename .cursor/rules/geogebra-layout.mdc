---
description: GeoGebra 插件布局与视图适配经验总结
globs:
  - "src/geogebra-renderer.ts"
  - "styles.css"
alwaysApply: false
---

# GeoGebra 插件布局经验

## 1. 不要用 overflow: hidden 裁间距

`overflow: hidden` 会同时裁掉四个方向。GeoGebra 内部可能有负 margin、transform 偏移、UI 输入栏等超出容器顶部的内容，一旦裁掉就"显示不全"。

**正确做法：**
- 3D 间距问题用 `clip-path: inset(0 0 GAP 0)` 只裁底部
- 2D/Graph 的 `@scale` 用 `margin-bottom: -GAP` 收缩间距
- 仅在 `@scale` + 3D 路径中使用 `overflow: hidden`（作用在 container 上）

## 2. CSS 全局规则必须限定模式作用范围

`.ggb-applet-container * { ... }` 会影响所有模式。GeoGebra 2D/Graph 的内部 UI 有自己的 transform 布局，被覆盖后会错位。

**正确做法：**
- 通过外层 container 加模式标记类（如 `.ggb-3d`）
- CSS 选择器写成 `.geogebra-container.ggb-3d .ggb-applet-container * { ... }`
- **绝不能**在 appletDiv 上加额外 class，会干扰 GeoGebra 的 `scaleContainerClass` 查找机制

## 3. ZoomToFit() 不可靠，用包围盒计算替代

GeoGebra 的 `ZoomToFit()` 在 3D 中基本不工作，2D 中也不留足够 padding 给标签。

**正确做法：**
- 遍历所有对象，用 `getObjectType()` 过滤，只取 `point`/`point3d` 的坐标
- **必须排除** `numeric`、`angle`、`boolean`、`function`、`text`、`list`、`image` 等非几何对象——它们的 `getXcoord()` 返回的不是空间坐标，会把包围盒撑得很大（如 Slider 值=120 被当成 x=120）
- 同时从 `getValueString()` 解析 `(x,y)` 或 `(x,y,z)` 格式坐标
- 每轴独立计算 padding：`range * 0.25 + 2`
- 3D 每轴独立范围（不用统一 maxRange），避免一个轴范围大时把其他轴也放大

## 4. 不同模式走不同代码路径

3D 有 `scaleContainerClass` 内部缩放（1400px → ~800px），2D/Graph 没有。混在一起会互相干扰。

**三条路径：**
| 路径 | 条件 | 处理 |
|------|------|------|
| A | 3D（ggbScale < 1） | clip-path 修间距 + 可选 @scale |
| B | 2D/Graph + @scale | 只做 CSS transform + negative margin |
| C | 2D/Graph 无 @scale | 不做任何调整 |

## 5. 参数校验不能省

`@zoom` 等用户参数可能为负数或零，导致坐标系反转（`xMin > xMax`）。

**正确做法：**
- `@zoom <= 0` 时自动从包围盒计算：找中心到最远点的距离 * 1.3 + 2
- 所有 `setCoordSystem` 调用前确保 min < max

## 6. scaleContainerClass 注意事项

GeoGebra 的 `scaleContainerClass` 参数指定一个 CSS 类名，GeoGebra 通过它找到容器元素并按容器宽度缩放。

- 不要修改该元素的 `className`（加额外 class 可能干扰查找）
- 不要在该元素上设置 `width: 100% !important` 等强制覆盖（会与 GeoGebra 内部缩放冲突）
- 3D 模式内部宽度设为 ≥1400px 才能让代数面板和 3D 视图并排显示

## 7. 测量时机

GeoGebra 异步加载，DOM 测量需要等待：
- `appletOnLoad` 回调后 GeoGebra 基本就绪
- 3D 视图调整需额外 800ms 延迟（内部初始化慢）
- `@scale` 测量高度需 800ms（2D）/ 600ms（3D）延迟
- 3D 自动适配需 1 秒后重试（首次可能不生效）
